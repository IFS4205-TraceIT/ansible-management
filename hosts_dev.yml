all:
  children:

    django:
      hosts:
        traceit-01-i:
        traceit-02-i:

    postgresql:
      hosts:
        traceit-01-i:
        traceit-02-i:

    nginx:
      hosts:
        traceit-04-i:

    grafana:
      hosts:
        traceit-06-i:

    cicd:
      hosts:
        traceit-05-i:

    vault:
      hosts:
        traceit-07-i:
  vars:
    # Username to ssh as
    ansible_user: sadm
