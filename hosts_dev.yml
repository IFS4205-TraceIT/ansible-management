all:
  children:

    django:
      hosts:
        traceit-01-i:

    postgresql:
      hosts:
        traceit-01-i:

    nginx:
      hosts:
        traceit-01-i:

    grafana:
      hosts:
        traceit-01-i:

    cicd:
      hosts:
        traceit-05-i:

    vault:
      hosts:
        192.168.1.207:
  vars:
    # Username to ssh as
    ansible_user: sadm
